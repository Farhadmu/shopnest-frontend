"use client";
import { useEffect,useState } from "react";
import { getCoupons,createCoupon,deleteCoupon,Coupon } from "@/lib/api/coupons";
export default function AdminCoupons(){const [items,setItems]=useState<Coupon[]>([]);const [code,setCode]=useState('');const [value,setValue]=useState('10');const load=()=>{getCoupons().then(setItems).catch(()=>setItems([]))};useEffect(()=>{load()},[]);const add=()=>createCoupon({code,type:'percentage',value:Number(value)}).then(()=>{setCode('');load()}).catch(()=>undefined);return <div><h1 className="text-3xl font-black">Platform Coupons</h1><p className="mt-2 text-sm text-muted">Create and manage promotional offers for the marketplace.</p><div className="mt-5 grid gap-2 sm:grid-cols-[1fr_140px_auto]"><input value={code} onChange={e=>setCode(e.target.value.toUpperCase())} placeholder="CODE10" className="rounded-xl border border-border bg-surface px-4 py-3"/><input type="number" value={value} onChange={e=>setValue(e.target.value)} className="rounded-xl border border-border bg-surface px-4 py-3"/><button onClick={add} className="rounded-xl bg-primary px-5 py-3 font-bold text-white">Create</button></div><div className="mt-5 grid gap-2">{items.map(c=><div key={c.id} className="flex items-center justify-between rounded-xl border border-border bg-surface p-4"><div><b>{c.code}</b><p className="text-xs text-muted">{c.type} · {c.value} · used {c.usedCount}</p></div><button onClick={()=>deleteCoupon(c.id).then(load)} className="text-xs font-bold text-error">Delete</button></div>)}</div></div>}
