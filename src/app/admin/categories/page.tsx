"use client";
import { useEffect,useState } from "react";
import { clientFetch,clientMutation } from "@/lib/core/client";
export default function AdminCategories(){const [items,setItems]=useState<any[]>([]);const [name,setName]=useState('');const load=()=>{clientFetch<any[]>('/categories').then(r=>setItems((r as any).data??r)).catch(()=>setItems([]))};useEffect(()=>{load()},[]);const add=()=>{if(!name.trim())return;clientMutation('/categories','POST',{name:name.trim()}).then(()=>{setName('');load()}).catch(()=>undefined)};return <div><h1 className="text-3xl font-black">Category Management</h1><p className="mt-2 text-sm text-muted">Keep product discovery structured and searchable.</p><div className="mt-5 flex gap-2"><input value={name} onChange={e=>setName(e.target.value)} placeholder="New category name" className="min-w-0 flex-1 rounded-xl border border-border bg-surface px-4 py-3"/><button onClick={add} className="rounded-xl bg-primary px-5 py-3 font-bold text-white">Add</button></div><div className="mt-5 grid gap-2 sm:grid-cols-2 lg:grid-cols-3">{items.map(c=><div key={c._id||c.id} className="flex items-center justify-between rounded-xl border border-border bg-surface p-4"><span className="font-bold">{c.name}</span><span className="text-xs text-muted">{c.slug}</span></div>)}</div></div>}
